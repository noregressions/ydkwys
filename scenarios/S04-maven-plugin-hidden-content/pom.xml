<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.noregressions.trace</groupId>
    <artifactId>maven-plugin-hidden-content</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>S04 Maven Plugin Hidden Content Trace Lab</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <trace.plugin.version>1.0.0</trace.plugin.version>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>dev.noregressions.trace</groupId>
                <artifactId>trace-injector-maven-plugin</artifactId>
                <version>${trace.plugin.version}</version>
                <executions>
                    <execution>
                        <id>inject-build-route</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>inject-route</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <compilerArgs>
                        <arg>--add-modules</arg>
                        <arg>jdk.httpserver</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>dev.noregressions.trace.s04.TraceServer</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
